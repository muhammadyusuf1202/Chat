{
  "name": "globalchat",
  "version": "1.0.0",
  "description": "GlobalChat - Real-time Web Chat Platform",
  "scripts": {
    "install": "cd backend && npm install",
    "start": "cd backend && node server.js",
    "dev": "cd backend && npm run dev"
  }
}
